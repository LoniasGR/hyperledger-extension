export const networkURL = 'http://147.102.19.6:8000';

export const balanceURL = `${networkURL}/balance`;
export const sla2BalanceURL = `${networkURL}/balance-sla2`;
export const vruURL = `${networkURL}/vru/incidents`;
export const partsURL = `${networkURL}/parts`;
export const initURL = `${networkURL}/init`;
